name: Probe Investing legacy BIST history
on:
  push:
    paths:
      - 'bist-s4-backtest/probe_investing_legacy.py'
      - '.github/workflows/probe-investing-legacy.yml'
  workflow_dispatch:
jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: pip install requests beautifulsoup4
      - run: python bist-s4-backtest/probe_investing_legacy.py
