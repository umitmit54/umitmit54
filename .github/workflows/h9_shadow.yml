name: GPT BORSA H9 Shadow
on:
  push:
    branches: [gpt-borsa-h9-20260818]
  pull_request:
    branches: [main]
permissions:
  contents: read
jobs:
  h9:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install KAP DOM parser
        run: pip -q install beautifulsoup4
      - name: Run preregistered H9 shadow with validated DOM parser
        run: python tools/h9_shadow_run.py
      - uses: actions/upload-artifact@v4
        with:
          name: gpt-borsa-h9-pead-shadow-dom
          path: |
            h9_results.csv
            h9_summary.csv
            h9_universe.csv
            h9_coverage.csv
          retention-days: 7
