name: GPT BORSA KAP Member-Type Diagnostic
on:
  push:
    branches: [gpt-borsa-shadow-20260818]
  pull_request:
    branches: [main]
permissions:
  contents: read
jobs:
  diag:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Inspect live KAP member types
        run: python tools/kap_company_types_diag.py
      - uses: actions/upload-artifact@v4
        with:
          name: gpt-borsa-kap-member-types-diag
          path: kap_company_types_diag.json
          retention-days: 2
